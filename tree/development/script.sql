CREATE SET TABLE RATINGS_CLEANED,FALLBACK( name VARCHAR(220), author VARCHAR(93), narrator VARCHAR(120), time_in_minutes INTEGER, releasedate DATE FORMAT 'YYYY-MM-DD', language VARCHAR(16), stars VARCHAR(31), price INTEGER ratings VARCHAR(10) ) UNIQUE PRIMARY INDEX(name);